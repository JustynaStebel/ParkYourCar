<%= form_for @car do |f| %>
  <% if @car.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@car.errors.count, "error") %> prohibited
        this car from being saved:
      </h2>
      <ul>
        <% @car.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= f.label :registration_number %></p>
  <%= f.text_field :registration_number %>

  <p><%= f.label :model %></p>
  <%= f.text_field :model %>

  <p><%= f.submit %></p>
<% end %>

<%= link_to 'Back to cars', cars_path %>
