<%= form_for [@parking, @place_rent] do |f| %>
  <% if @place_rent.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@place_rent.errors.count, "error") %> prohibited
        this place rent from being saved:
      </h2>
      <ul>
        <% @place_rent.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p><%= f.label :starts_at %></p>
  <%= f.datetime_select :starts_at %>

  <p><%= f.label :ends_at %></p>
  <%= f.datetime_select :ends_at %>

  <p><%= f.label :car_id %></p>
  <%= f.select :car_id, options_from_collection_for_select(current_person.cars, 'id', 'model') %>

  <p><%= f.submit %></p>

<% end %>

<%= link_to 'Back to place rents', place_rents_path %>

